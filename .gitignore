# --- CRITICAL SECRETS (NEVER REMOVE THESE) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- DEPENDENCIES ---
# We don't upload these because they are huge; 
# others will run 'npm install' to get them.

# --- LOGS & DIAGNOSTICS ---
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- SYSTEM & IDE FILES ---
.DS_Store
.vscode/
.idea/
Thumbs.db

# --- RUNTIME DATA ---
pids
*.pid
*.seed
*.pid.lock
